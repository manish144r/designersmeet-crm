# Production env — real backend, no demo mode.
# The backend serves this frontend on the same origin, so /api is same-origin.
VITE_DEMO_MODE=false
VITE_AUTH_MODE=prod
# VITE_API_URL is intentionally blank — same-origin /api proxy works.
# VITE_API_URL=
